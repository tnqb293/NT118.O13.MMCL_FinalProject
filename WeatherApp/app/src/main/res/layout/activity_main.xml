<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:layout_marginLeft="16dp"
    android:layout_marginRight="16dp"
    android:layout_marginTop="16dp"
    android:layout_marginBottom="16dp">
    <RelativeLayout
        android:id="@+id/rl_logo_background"
        android:layout_width="42dp"
        android:layout_height="42dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:background="@drawable/bg_button_login">
        <ImageView
            android:id="@+id/iv_icon_logo"
            android:layout_width="30dp"
            android:layout_height="28dp"
            android:layout_centerInParent="true"
            android:background="@drawable/ic_air_monitor" />
    </RelativeLayout>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toRightOf="@id/rl_logo_background"
        app:layout_constraintTop_toTopOf="@id/rl_logo_background"
        app:layout_constraintBottom_toBottomOf="@id/rl_logo_background"
        android:textSize="26sp"
        android:textColor="@color/black"
        android:text="@string/app_tittle"
        android:textStyle="bold"
        android:layout_marginStart="10dp"
        />
    <FrameLayout
        android:id="@+id/fl_login"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/rl_logo_background"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp"
        />
</androidx.constraintlayout.widget.ConstraintLayout>